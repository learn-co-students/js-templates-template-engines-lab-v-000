function createPost() {
  let pageFn = _.template(document.getElementById('page-template').innerHTML);
  let postFn = _.template(document.getElementById('post-template').innerHTML);
}

function postComment() {
  let comment = _.template(document.getElementById('comment-template').innerHTML);
}
